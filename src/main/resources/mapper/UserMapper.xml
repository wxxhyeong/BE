<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.be.user.mapper.UserMapper">
    <insert id = "insert" parameterType="UserRegisterResDto">
        insert into user(user_id, user_name, user_email, user_password, user_birth,
                         user_gender, user_reg_date)
        values (#{userID}, #{userEmail}, #{userName}, #{userPassword}, #{userBirth},
                #{userGender}, #{userRegDate});
    </insert>

    <select id="selectOneByUserID" parameterType="String" resultType="User">
        select * from user where user_id = #{userID};
    </select>

    <select id="selectOneByUserEmail" parameterType="String" resultType="User">
        select * from user where user_email = #{userEmail};
    </select>

</mapper>